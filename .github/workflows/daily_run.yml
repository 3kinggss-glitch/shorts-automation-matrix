name: Daily Faceless Video Arbitrage Engine

on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch: 

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the code matrix
        uses: actions/checkout@v3

      - name: Initialize Free Python Environment
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install FFmpeg Video Engine
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Inject Pipeline Dependencies
        run: |
          pip install --upgrade pip
          pip install requests google-genai edge-tts openai

      - name: Execute Autonomous Production Circuit
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          TIKTOK_SESSION_COOKIE: ${{ secrets.TIKTOK_SESSION_COOKIE }}
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
        run: python main.py
